# 2026-08-31 작업일지

## 완료

- Playwright Chromium 기반 QA 자동화 프로젝트 생성
- QualityStream 단방향 순차 TC 방침 확정
- 실패 로그, 브라우저 콘솔 오류, 영상 및 트레이스 저장 구성
- 메뉴별 모듈과 테스트 입력값 분리 관리 구조 추가
- 기존 스모크 테스트를 `tests/modules/smoke/`의 Page/Flow/Spec 구조로 분리
- GitHub 저장소 `pizza94/QA_AUTO_Quality`를 배포 대상으로 확정
- 정리된 프로젝트 구조를 GitHub `main` 브랜치에 최초 푸시
- 실제 로그인 화면의 접근성 요소를 확인하고 로그인 Page/Flow/Spec 모듈 구현
- 제공된 TC 계정으로 실제 로그인 성공 및 Data Portal 홈 진입 검증
- 로그인 TC 입력·기대값을 YAML로 분리하고 환경변수 참조 기반 자격증명 로더 연결
- TC ID를 참고 형식에 맞춰 로그인 `TC-001`, 품질관리 진입 `TC-002`로 정리
- 품질관리 카드 클릭 후 QualityStream 4.3 대시보드와 상위 메뉴 진입 TC 구현
- 메타데이터수집관리 신규 등록 `TC-003` 구현 및 증가형 예약명 생성 규칙 추가
- `TC-003` 재실행으로 `수집테스트자동2` 신규 예약 생성 및 목록 검증 통과
- `TC-004` 즉시실행 후 목록 상태와 하단 수집이력의 신규 완료 건 검증 통과
- 실패 로그 리포터를 Asia/Seoul 날짜별 하위 폴더 저장 방식으로 보강하고 오류 유형·원인·권장 조치 자동 분석 추가
- `TC-005` 검증대상관리 미반영 신규 대상 상위 2건을 큐에이/QA상품으로 반영하고 결과 검증 통과
- 사용자 요구 변경에 따라 TC-005의 향후 반영 대상을 상위 2건에서 상위 1건으로 축소하고 실제 재실행은 보류
- 상위 1건으로 변경한 TC-005 실제 재실행 및 반영 결과 검증 통과
- 화면 시연은 직접 브라우저 조작 대신 명시적인 Playwright headed 명령으로만 수행하도록 규칙 확정
- `TC-006` 최근 반영 테이블 최신 1건의 테이블 실행과 전체 컬럼 실행·컬럼 설정 저장 검증 통과
- TC-006 기본정보 저장 후 페이지 새로고침과 테이블실행여부 체크 유지 검증 단계 추가
- TC-006 컬럼 저장 후 페이지 새로고침과 전체 실행여부·컬럼 체크 유지 검증 단계 추가
- UI 화면 시연 명령을 TC-001부터 TC-006까지 순차 실행하는 단일 `test:headed` 명령으로 통합
- UI 화면 시연 명령이 향후 추가되는 번호형 TC spec도 자동 탐색해 TC 번호순으로 실행하도록 개선
- 전체 UI 시연을 TC별 독립 실행에서 단일 브라우저 세션의 절차형 실행으로 변경하고 TC-001 이후 반복 로그인과 이전 단계 재실행 제거
- 모든 TC가 Git 제외 로컬 YAML에서 로그인 URL과 계정 정보를 공통으로 읽도록 변경
- 이전 메뉴 DOM과 조회 상태가 유지되는 제품 특성에 맞춰 메뉴 진입 시 해당 영역 검색 버튼을 초기 실행하고 중복 ID 선택자를 현재 메뉴 영역으로 제한
- 절차 실행 중 이미 열린 대메뉴를 다시 눌러 접지 않도록 하위 메뉴가 숨겨진 경우에만 대메뉴를 펼치도록 수정
- 누적된 이전 메뉴 DOM과 충돌하지 않도록 세 메뉴의 컨트롤·그리드·탭·필터·버튼·상세 패널 선택자를 각 활성 메뉴 영역으로 전면 제한
- 장기 재실행을 위한 구조 문서, SemVer 변경이력, 사전점검, CSV/전체 절차 동기화 검사, 헤드리스 전체 실행 명령, 실행 버전·Git 커밋 기록 기능 추가
- `tests/test-context`에 장기 중단 후 재시작 진입점과 기계 판독형 원본 경로·불변 규칙을 추가하고 사전점검과 연결
- 분산된 실행 컨텍스트를 `tests/test-context/project-context.yml` 단일 기준 파일로 통합하고 README와 CODEX_CONTEXT는 안내 포인터로 축소
- README에 작업 종료 전 원본 업데이트 체크리스트와 다음 대화에서 그대로 사용할 전체 실행·UI 시연·신규 TC 요청문 추가
- 메타데이터수집관리 공통 화면 탭을 콘텐츠 영역에서 잘못 찾던 문제 수정, 목록 검색은 영역 내부로 유지하고 선택자 동작 제한시간 추가
- 버전 관리는 SemVer와 검증 태그, 일반 변경은 Git 커밋, 날짜별 과정은 작업일지로 관리하도록 확정
- TC-003 저장 후 검색 버튼으로 목록을 갱신해 신규 예약을 검증하도록 수정하고 TC-004 최종 결과는 신규 수집이력 완료 상태를 기준으로 유지
- 저장된 수집테스트자동7이 화면에 존재하지만 영역이 중복된 has 선택자로 찾지 못한 문제를 확인하고 세 메뉴 그리드 행의 내부 셀을 상대 선택자로 수정
- TC-005 검증대상관리와 TC-006 테이블 화면 탭도 공통 탭 컨테이너에서 보이는 탭을 확인하도록 수정
- TC-004 수집 대기를 반복 UI 검색에서 인증된 상태조회 API 10초 간격 조회로 변경하고 완료 후 목록·수집이력만 최종 확인하도록 개선
- TC-007 프로파일링 > 컬럼분석에서 시스템 큐에이·업무 QA상품·실행이력 N과 TC-006 대상 테이블로 조회하고 설정 컬럼 ID 전체를 비교하는 자동화 추가
- TC-007 검색 기준을 TC-006 테이블ID 정확검색으로 확정하고 실행여부·컬럼 체크가 모두 저장된 컬럼ID 집합만 조회 결과와 비교하도록 보완
- TC-006 실제 테이블ID와 체크 컬럼ID는 전체 테스트 세션 메모리로만 TC-007에 전달하고 별도 파일에는 저장하지 않도록 확정
- TC-007 테이블ID 정확검색 렌더링 대기와 체크 컬럼ID 집합 비교 개별 검증 통과
- TC-007 범위를 일치한 컬럼ID 전체 선택과 컬럼분석 실행 요청까지 확장
- TC-007 컬럼ID 비교와 전체 선택·실행 요청 개별 백그라운드 검증 통과
- 정상 출력 최소화, 핵심 오류 우선 분석, 개별 TC 우선 검증, 문서 일괄 갱신을 토큰 절약 규칙으로 AGENTS와 단일 프로젝트 컨텍스트에 저장

## 결정

- TC 흐름: 화면 진입 → 값 입력 → 실행 → 결과 검증 → 다음 단계
- 기본 검증은 헤드리스로 실행하고, 사용자가 요청할 때만 브라우저 UI를 시연
- TC 정의·절차·기대결과·실행 이력은 `tests/test-cases/qa-test-cases.csv`에서 관리
- 실제 자동화 입력값과 검증값은 `tests/test-data/<menu>.yml`에서 관리
- URL과 계정 등 민감정보는 환경변수로만 관리
- 로그인 URL도 `PLAYWRIGHT_LOGIN_URL` 환경변수로만 주입
- 완료 작업도 자동 커밋하지 않고, 사용자가 요청할 때만 커밋·푸시 수행
- TC 데이터는 YAML로 관리하되 실제 자격증명은 평문으로 저장하지 않음
- YAML 자격증명 필드에는 환경변수 참조명만 허용하도록 `AGENTS.md` 지침 보강
- 참고 프로젝트 형식에 맞춰 TC 문서 CSV와 자동화 데이터 YAML을 역할별로 분리
- TC CSV를 `tests/test-cases/`로 이동해 모든 테스트 자산을 `tests/` 아래로 통합
- 루트 `.env.example`을 `tests/test-data/`로 이동하고 비코드 테스트·환경정보 파일 저장 위치를 통일
- 수집 예약명은 기존 `수집테스트자동N`의 최대 숫자 다음 값으로 생성
- TC-003 입력값은 DB카탈로그/ORA19C(META42_DEV), 특정일 2036-08-31, 특정시 15시 30분으로 고정
- 품질관리 카드의 숨겨진 사이드 링크 대신 화면에 보이는 정확한 텍스트 요소를 선택
- 수집 등록 옵션 선택 시 예약명이 초기화되는 동작을 확인해 예약명을 마지막에 입력하도록 보정
- TC-002와 TC-003 헤드리스 Chromium 통과, TC-003 실제 저장 및 목록 검증 완료
- 최신 `수집테스트자동N` 예약 선택 후 즉시실행과 상태 변경을 검증하는 TC-004 구현
- TC-004 즉시실행 재확인 생략 승인 및 수집상태 검증 대기시간 5분 적용
- 작업이 빠르거나 목록 상태 갱신이 늦어 `수집중`을 놓칠 수 있으므로, TC-004는 `수집중` 관찰 또는 실행 전후 이력 증가를 실행 증거로 사용하고 최종적으로 목록과 최신 수집이력의 `완료`를 함께 검증

## 다음 작업

- 다음 자동화 대상 TC 확정
- TC-008 컬럼분석 실행 결과에 컬럼명·데이터타입 기반 매핑룰을 적용하고 재실행하는 자동화 절차를 추가했다.
- TC-009에서 증가형 프로파일링 점검작업을 등록하고 TC-006 대상 테이블 추가 및 작업명 재조회를 검증했다.
- TC-004가 10초마다 검색 버튼으로 목록을 갱신해 수집 완료를 확인한 뒤 하단 수집이력을 검증하도록 수정했다.
- 컬럼분석에서 동일 테이블의 이전 실행 행이 남아 있을 때 검색 결과로 오인하던 타이밍 문제를 수정했다.
